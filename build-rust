#!/bin/bash

flag=-r

cargo build $flag --bin diff
cargo build $flag --bin receive
cargo build $flag --bin index --target aarch64-linux-android
cargo build $flag --bin send --target aarch64-linux-android
